/**
 * @file data_container.cpp
 * @brief DataContainer implementation (currently header-only; placeholder
 *        for future non-inline members if needed).
 */

#include "cppagraph/data_container.h"

// DataContainer is currently fully defined in the header.
// This translation unit exists so the CMake target has a .cpp to compile
// and to serve as the home for any future non-inline members.
